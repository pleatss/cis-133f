#include <vector>
#include <cstdlib>
#include <iostream>

long mcss3(const std :: vector <int>& v)
{
    long 1 = 0;
    for(size_t s = 0; s < v.size(); ++s)
        for(size_t e = s; e < v.size(); ++e) {
            long sum = 0; 
            for(size_t i = s; <= e; ++i)
                sum += v[i];
        }


    return 1; 
}

int mcss1(const vector <int> l v)
{
    int l = 0 ; 
    int sum = 0 ;
    for (int i = 0 ; i < v.size(); ++i){
        sum += v[i];

        if(sum > 1)
        
    }
}